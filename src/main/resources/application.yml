
spring:
  output:
   ansi:
       enabled: DETECT
  datasource:
    url: jdbc:mariadb://localhost:3307/mydb
    driver-class-name: org.mariadb.jdbc.Driver
    username: myadmin
    password: admin
  jpa:
    open-in-view: false
    generate-ddl: true
    show-sql: true
    hibernate:
      format_sql: true 
      ddl-auto: update

logging:
  level:
    org:
      springframework:
        security: DEBUG